```
$ dotnet new console -n [Enter name in quotes] --use-program-main
> Goto csproj and set nullable to disable
> In VSCode open command pallete and generate build assets if necessary
$ dotnet new sln -n [Enter name in quotes]
$ dotnet sln add .\path_to_csproj.csproj 
```